service cloud.firestore {
  match /databases/{database}/documents {
    // Allow user access to only their own document.
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}
